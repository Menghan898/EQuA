% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossfit-equa.R
\name{crossfit_equa}
\alias{crossfit_equa}
\title{Repeated cross-fitted EQuA profiles}
\usage{
crossfit_equa(
  chronological_age,
  estimated_age,
  covariates = NULL,
  method = c("kernel_end", "po", "kernel_ex"),
  probs = seq(0.1, 0.9, by = 0.1),
  bandwidth = NULL,
  folds = 5,
  repeats = 50,
  seed = 2026,
  n_strata = 10,
  aggregate = c("median", "mean"),
  na_action = c("omit", "fail"),
  support_action = c("warn", "na", "error", "nearest"),
  min_effective_n = 10,
  nearest_k = 30,
  keep_repeats = FALSE,
  keep_fold_assignments = TRUE
)
}
\arguments{
\item{chronological_age}{Numeric vector of chronological ages, in the same
units as \code{estimated_age}.}

\item{estimated_age}{Numeric vector of scalar ages from an existing
estimator, in the same units as \code{chronological_age}.}

\item{covariates}{Optional numeric vector, matrix, or data frame. Required
only for \code{method = "kernel_ex"}.}

\item{method}{One of \code{"kernel_end"} (End-EQuA), \code{"po"} (Po-EQuA), or
\code{"kernel_ex"} (Ex-EQuA).}

\item{probs}{Strictly increasing percentile levels between zero and one.}

\item{bandwidth}{Optional bandwidth vector in the original units of the
conditioning variables. The default applies the documented rule separately
to every conditioning dimension.}

\item{folds}{Number of folds.}

\item{repeats}{Number of repeated fold assignments.}

\item{seed}{Integer base seed. Repeat \code{r} uses \code{seed + 100 * r}, matching the
reproducibility workflow.}

\item{n_strata}{Maximum number of chronological-age strata.}

\item{aggregate}{Pointwise aggregation across repeats, either \code{"median"} or
\code{"mean"}.}

\item{na_action}{Either \code{"omit"} or \code{"fail"}.}

\item{support_action}{Default prediction behavior in low-support regions:
\code{"warn"}, \code{"na"}, \code{"error"}, or the explicitly requested \code{"nearest"}
fallback.}

\item{min_effective_n}{Positive threshold used to flag low effective kernel
sample size.}

\item{nearest_k}{Number of neighbors used only when
\code{support_action = "nearest"}.}

\item{keep_repeats}{Whether to retain all repeat-level qEA and qEAA arrays.}

\item{keep_fold_assignments}{Whether to retain the fold-assignment matrix.}
}
\value{
An object with classes \code{equa_crossfit} and \code{equa_profile}.
Cross-fitting results contain participant-level inputs and profiles; review
disclosure risk before saving or sharing them.
}
\description{
Produces strictly out-of-fold qEA and qEAA profiles using repeated,
chronological-age-stratified K-fold cross-fitting.
}
\examples{
calibration <- subset(
  simulated_equa_data,
  sample_role == "calibration"
)
profile <- crossfit_equa(
  chronological_age = calibration$chronological_age,
  estimated_age = calibration$estimated_age,
  method = "kernel_end",
  probs = c(0.25, 0.5, 0.75),
  folds = 5,
  repeats = 2,
  seed = 2026
)
profile

}
